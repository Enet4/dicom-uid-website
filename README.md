# DICOM UID

A simple website about DICOM UIDs, with a UID generator.

## Building

With a stable Rust toolchain,
Install `wasm-bindgen-cli`
(specify the version indicated in Cargo.toml),
then run the script `build-wasm-release.sh`
to build the dynamic part of the site.

Then you may serve this directory using any static HTTP server.
